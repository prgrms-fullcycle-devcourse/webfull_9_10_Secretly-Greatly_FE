# REST·소켓 공통. 항상 "/api"까지 포함한 절대 URL.
# REST → 그대로 baseURL, 소켓 → 끝의 "/api"를 떼어 오리진으로 사용.
# 운영(Vercel)은 https://secretlygreatly.duckdns.org/api 로 설정.
NEXT_PUBLIC_API_URL=http://localhost:3000/api
NEXT_PUBLIC_API_MOCKING=false